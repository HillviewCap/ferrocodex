# Ferrocodex Environment Variables
# Copy this file to .env if you need to customize development settings

# Optional: Remote development host for Tauri
# Only needed if developing on a remote machine
# TAURI_DEV_HOST=false

# Note: Ferrocodex is designed as an offline-first desktop application
# and does not require external API keys or service configurations.
# All application data is stored locally in an encrypted SQLite database.